import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import VinhosList from './TelaCatalogo';

export default function TelaCatalogo(){
  return(
    <VinhosList/>
  );
}